# Use official Redis image as a base or Alpine-based image
FROM redis:alpine

# Set metadata for the image (replaces MAINTAINER)
LABEL maintainer="Anand Mani Sankar"

# Expose Redis port
EXPOSE 6379

# Entrypoint is already defined in the base image
